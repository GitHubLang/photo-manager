# 摄影素材管理系统

基于 AI 的摄影素材管理、Web 图片浏览、评分和文案生成系统。

---

## 功能特性

| 功能 | 说明 |
|------|------|
| 📁 目录树浏览 | 按日期整理素材，支持搜索、分页、排序 |
| 🔍 全局搜索 | 搜索文件名、描述、标签 |
| ⭐ AI 评分 | 本地 LLM / MiniMax 双模型，六维度评分 |
| 🏷️ AI 描述生成 | 自动生成图片描述和标签 |
| 📝 每日主题总结 | AI 分析当日摄影主题 |
| 📱 抖音/小红书文案 | 一键生成可发布文案 |
| 🎞️ 照片合集 | 标签聚类分组，抖音风格上下滑动浏览 |
| 🎵 BGM 配乐 | 预置 200 首免费 Mixkit 曲库 + 本地音乐兜底 |
| ❤️ 收藏 | 合集收藏（含 BGM/标题/文案/tags 持久化） |
| 🗑️ 软删除 | 磁盘文件删除后自动标记，所有查询过滤 |
| 🎨 LUT 克隆 | 3D LUT 色彩风格迁移 |

---

## 系统架构

```
┌──────────────────────────────────────────────────────────────────┐
│                     前端 (React 18 + Vite 8 + Ant Design)         │
│  ┌────────────────┐    ┌────────────────────────────────────────┐│
│  │  📁 文件夹浏览  │    │          右侧图片画廊                    ││
│  │  (FolderPage)  │    │  - 搜索/筛选/排序/分页                   ││
│  │                │    │  - 评分/文案/下载                        ││
│  │   全局搜索     │    │  - 图片预览/批量操作                      ││
│  └────────────────┘    └────────────────────────────────────────┘│
│                                                                   │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │  🎞️ 照片合集 (CollectionPage, 抖音风格滑动)                   ││
│  │  🎵 BGM 播放 (BgmPlayer, mood 自动匹配)                      ││
│  │  🎨 LUT 克隆  ⚙️ 设置  🤖 模型管理                           ││
│  └──────────────────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                    后端 (Python 3.11 + FastAPI)                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────────────────┐   │
│  │ 图片扫描  │ │ LLM 评分 │ │ 主题/文案 │ │ 照片合集/收藏/BGM  │   │
│  │ folders/  │ │ scoring/ │ │ daily/   │ │ collections/bgm/  │   │
│  │ images/   │ │          │ │          │ │ settings/models/  │   │
│  └──────────┘ └──────────┘ └──────────┘ └───────────────────┘   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │  统一数据访问层 (db.py → DB 类, 80+ 方法)                     ││
│  │  异步 LLM (httpx.AsyncClient + asyncio.gather)               ││
│  │  is_deleted 软删除标记                                        ││
│  └──────────────────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────────────┘
                              │
         ┌────────────────────┼────────────────────┐
         ▼                    ▼                    ▼
   ┌───────────┐       ┌───────────┐        ┌──────────────┐
   │   MySQL   │       │ E:\图像\  │        │ MiniMax API  │
   │   8.0     │       │ (素材库)  │        │ 本地 LLM     │
   └───────────┘       └───────────┘        └──────────────┘
```

---

## 数据库设计

```sql
-- 图片基础信息
CREATE TABLE images (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    file_path VARCHAR(500) NOT NULL UNIQUE,
    filename VARCHAR(255) NOT NULL,
    folder_date DATE,
    folder_path VARCHAR(500),
    file_size BIGINT,
    width INT DEFAULT 0,
    height INT DEFAULT 0,
    orientation ENUM('landscape', 'portrait', 'square'),
    perceptual_hash VARCHAR(64),
    theme_tags VARCHAR(255),
    is_deleted TINYINT(1) DEFAULT 0,    -- 软删除标记
    created_at DATETIME,
    indexed_at DATETIME,
    INDEX idx_folder_date (folder_date)
);

-- 图片评分（六维度）
CREATE TABLE image_scores (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    image_id BIGINT NOT NULL,
    total_score DECIMAL(5,2),
    impact_score DECIMAL(5,2),      impact_analysis TEXT,      impact_suggestion TEXT,
    composition_score DECIMAL(5,2), composition_analysis TEXT, composition_suggestion TEXT,
    sharpness_score DECIMAL(5,2),   sharpness_analysis TEXT,   sharpness_suggestion TEXT,
    exposure_score DECIMAL(5,2),    exposure_analysis TEXT,    exposure_suggestion TEXT,
    color_score DECIMAL(5,2),       color_analysis TEXT,       color_suggestion TEXT,
    uniqueness_score DECIMAL(5,2),  uniqueness_analysis TEXT,  uniqueness_suggestion TEXT,
    raw_response JSON,
    llm_model VARCHAR(100),
    scored_at DATETIME,
    FOREIGN KEY (image_id) REFERENCES images(id) ON DELETE CASCADE,
    UNIQUE KEY uk_image_id (image_id),
    INDEX idx_total_score (total_score)
);

-- 图片描述 + 标签
CREATE TABLE image_descriptions (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    image_id BIGINT NOT NULL UNIQUE,
    description TEXT,
    tags VARCHAR(500),
    llm_model VARCHAR(100),
    created_at DATETIME,
    FOREIGN KEY (image_id) REFERENCES images(id) ON DELETE CASCADE
);

-- 评分任务队列
CREATE TABLE score_tasks (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    image_id BIGINT NOT NULL,
    status ENUM('pending','processing','completed','failed') DEFAULT 'pending',
    model VARCHAR(50),
    error_message TEXT,
    created_at DATETIME,
    completed_at DATETIME,
    FOREIGN KEY (image_id) REFERENCES images(id) ON DELETE CASCADE,
    INDEX idx_status (status)
);

-- 每日主题
CREATE TABLE daily_themes (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    date DATE UNIQUE,
    theme_title VARCHAR(200),
    theme_description TEXT,
    photo_count INT,
    total_score_avg DECIMAL(5,2),
    keywords VARCHAR(500),
    created_at DATETIME
);

-- 文案推荐组合
CREATE TABLE photo_sets (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    date DATE,
    set_type ENUM('douyin','xiaohongshu','weibo'),
    cover_image_id BIGINT,
    caption_title VARCHAR(200),
    caption_body TEXT,
    hashtags VARCHAR(500),
    image_ids JSON,
    llm_model VARCHAR(100) DEFAULT 'local',
    created_at DATETIME,
    FOREIGN KEY (cover_image_id) REFERENCES images(id) ON DELETE SET NULL
);

-- 照片合集（标签聚类 + LLM 文案）
CREATE TABLE photo_collections (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(200) NOT NULL DEFAULT '',
    description TEXT,
    tags VARCHAR(500),
    theme_type VARCHAR(50) DEFAULT 'theme',
    theme_value VARCHAR(100) DEFAULT '',
    photo_paths JSON,
    photo_ids JSON,
    cover_path VARCHAR(500),
    is_favorite TINYINT(1) DEFAULT 0,     -- 收藏状态
    bgm_track VARCHAR(300) DEFAULT '',      -- 收藏时的 BGM 曲目
    llm_model VARCHAR(100) DEFAULT 'local',
    created_at DATETIME,
    INDEX idx_is_favorite (is_favorite),
    INDEX idx_created_at (created_at),
    INDEX idx_theme_value (theme_value)
);

-- 用户设置（键值对）
CREATE TABLE user_settings (
    `key` VARCHAR(50) PRIMARY KEY,
    `value` TEXT
);

-- 文案指令历史
CREATE TABLE instruction_history (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    instruction VARCHAR(500) NOT NULL,
    set_type ENUM('douyin','xiaohongshu') NOT NULL,
    created_at DATETIME,
    INDEX idx_set_type (set_type)
);

-- 应用状态（浏览位置持久化）
CREATE TABLE app_state (
    id INT PRIMARY KEY DEFAULT 1,
    last_folder_path VARCHAR(500),
    last_page INT DEFAULT 1,
    last_sort_by VARCHAR(50) DEFAULT 'filename',
    last_sort_order VARCHAR(10) DEFAULT 'asc',
    updated_at DATETIME
);
```

---

## AI 评分维度

| 维度 | 权重 | 说明 |
|------|------|------|
| 冲击力 (Impact) | 25% | 情感反应、视觉震撼力 |
| 构图 (Composition) | 25% | 三分法、黄金分割、平衡 |
| 清晰度 (Sharpness) | 20% | 对焦清晰、无模糊 |
| 曝光 (Exposure) | 15% | 无过曝/欠曝 |
| 色彩 (Color) | 10% | 白平衡、饱和度 |
| 独特性 (Uniqueness) | 5% | 原创性、独特视角 |

---

## 评分标准参考

专业摄影比赛评分体系：

| 等级 | 分数 | 说明 |
|------|------|------|
| 卓越 | 80-100 | 专业水准 |
| 良好 | 60-79 | 业余优秀 |
| 一般 | 40-59 | 可接受，有提升空间 |
| 较差 | <40 | 明显问题 |

---

## 模型支持

| 模型 | 用途 | 配置 |
|------|------|------|
| 本地 Gemma 4-26b | 评分、描述生成、文案 | 本地 (见 .env) |
| MiniMax M2.7 | 评分、描述生成、文案 | API Token |

---

## 项目结构

```
photo-manager/
├── backend/
│   ├── config.py              # 配置（.env → DB/LLM/路径）
│   ├── database.py           # MySQL 连接池 + 建表
│   ├── db.py                 # [统一] 数据访问层 (DB 类, 80+ 方法)
│   ├── main.py               # FastAPI 入口, 路由注册
│   ├── core/
│   │   └── model_router.py   # 本地/云端模型判断
│   ├── routers/              # API 路由 (10 个)
│   │   ├── folders.py        # 文件夹扫描/列表
│   │   ├── images.py         # 图片 CRUD/搜索/代理
│   │   ├── scoring.py        # AI 评分
│   │   ├── daily.py          # 每日主题/文案
│   │   ├── collections.py    # 照片合集
│   │   ├── bgm.py            # 本地音乐扫描/服务
│   │   ├── models.py         # 模型管理
│   │   ├── settings.py       # 用户设置
│   │   ├── benchmark.py      # 性能测试
│   │   └── lut.py            # LUT 克隆
│   └── services/             # 业务逻辑 (5 个)
│       ├── collection_service.py  # 合集: 标签聚类 + LLM 文案 + 收藏
│       ├── image_scanner.py       # 磁盘扫描 + 缩略图 + is_deleted 标记
│       ├── daily_theme.py         # 每日主题/推荐文案 (async)
│       ├── llm_scorer.py          # LLM 评分+描述 (async)
│       └── benchmark_service.py   # 性能测试
│
├── frontend/
│   ├── index.html            # viewport-fit=cover
│   ├── public/bgm/            # 预置 Mixkit BGM (200首)
│   │   ├── manifest.json
│   │   └── cinematic/ lofi/ upbeat/ calm/ ambient/
│   └── src/
│       ├── App.jsx            # 根组件 (210行, 路由 + 布局)
│       ├── App.css            # 全局样式
│       ├── api/imageApi.js    # 所有 API 调用
│       ├── hooks/             # 自定义 hooks
│       │   ├── useImages.js   # 图片列表/分页/排序
│       │   ├── useSearch.js   # 搜索
│       │   ├── useScore.js    # 评分任务
│       │   └── useCaption.js  # 文案生成
│       ├── config/menu.js     # 菜单配置 (数据驱动)
│       ├── pages/
│       │   ├── FolderPage.jsx      # 文件夹浏览 (120行, 从 App.jsx 拆出)
│       │   ├── CollectionPage.jsx  # 照片合集 (400行)
│       │   ├── ModelPage.jsx       # 模型管理
│       │   ├── GeneralPage.jsx     # 通用设置
│       │   └── LutPage.jsx         # LUT 克隆
│       └── components/
│           ├── bgm/BgmPlayer.jsx        # 背景音乐 (mood 检测)
│           ├── collection/              # 合集子组件
│           │   ├── CollectionSlide.jsx   # 照片滑动层
│           │   ├── CollectionHeader.jsx  # 顶部栏
│           │   ├── CollectionFooter.jsx  # 底部文案
│           │   └── CollectionLoading.jsx # 加载/空状态
│           ├── layout/          # 布局 (SideMenu, TopToolbar, BottomTabs, MobileDrawers)
│           ├── modals/          # 弹窗 (7个)
│           ├── score/           # 评分面板
│           └── caption/         # 文案面板
│
├── 摄影素材管理系统设计文档.md   # 本文件
└── DESIGN.md                    # 架构设计规范
```

---

## 快速启动

### 1. 后端

```bash
cd D:\MySoftware\photo-manager\backend

# 安装依赖
pip install -r requirements.txt

# 启动服务（会自动创建数据库和表）
python main.py
```

服务地址：`http://localhost:8000`
API 文档：`http://localhost:8000/docs`

### 2. 前端

```bash
cd D:\MySoftware\photo-manager\frontend

# 创建 React 项目
npm create vite@latest . -- --template react

# 安装依赖
npm install
npm install antd @ant-design/icons

# 启动开发服务器
npm run dev
```

前端地址：`http://localhost:5173`

---

## 核心 API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/folders` | 目录树 |
| POST | `/api/folders/scan-all` | 全量扫描（标记 is_deleted） |
| GET | `/api/folders/{path}/images` | 文件夹图片列表（分页） |
| GET | `/api/image/proxy/{path}` | 图片文件代理 |
| POST | `/api/images/score` | 创建评分任务 |
| GET | `/api/score-tasks` | 评分任务列表/状态 |
| GET | `/api/search` | 全局搜索 |
| POST | `/api/daily-theme/{date}/generate` | 生成每日主题 |
| POST | `/api/photo-sets/caption` | 生成文案 |
| POST | `/api/collections/generate` | 生成合集（仅分组，1-2s） |
| POST | `/api/collections/refresh-meta` | 刷新合集文案（async LLM） |
| GET | `/api/collections` | 合集列表（?favorite_only） |
| POST | `/api/collections/{id}/favorite` | 收藏切换（含 bgm_track） |
| DELETE | `/api/collections/{id}` | 删除合集 |
| POST | `/api/collections/clear` | 清空非收藏合集 |
| GET | `/api/models` | 模型列表 |
| GET/POST | `/api/settings` | 用户设置 |
| GET | `/api/bgm/local` | 本地音乐列表 |
| GET | `/api/bgm/local/{file}` | 本地音乐文件 |
| POST | `/api/lut/clone` | LUT 颜色克隆 |

---

## 使用流程

```
1. 启动后端 → 启动前端
2. 打开前端页面（见 vite 配置或启动输出）
3. 点击「扫描」导入所有图片到数据库
4. 选择一个日期文件夹浏览图片
5. 选择图片 → 批量评分 → LLM 六维度打分
6. 可选：生成每日主题 / 生成文案（抖音/小红书风格）
7. 进入「照片合集」→ 自动分组 → 抖音式滑动浏览
8. 喜欢的合集点 ❤️ 收藏 → 在「我的收藏」中查看
```

## 关键设计模式

### 数据访问层 (db.py)
所有 SQL 集中在 `backend/db.py` 的 `DB` 类中，80+ 命名方法。
Router 和 Service 不直接写 SQL，通过 `DB.xxx()` 调用。

### 异步 LLM 调用
`httpx.AsyncClient` + `asyncio.gather` 替代同步 `requests.post`。
LLM 调用不再阻塞 FastAPI event loop，请求可并发处理。

### is_deleted 软删除
扫描时检查磁盘文件是否存在 → 标记 `is_deleted=0/1` → 所有查询统一过滤 `is_deleted=0`。
删了磁盘文件不会出现裂图。

### 合集懒加载 + 预刷新
生成合集 → 只分组不入库文案 (1-2s) → 前端显示占位 "⏳ 生成中..."
→ 自动刷新前 5 个合集文案 (asyncio.gather 并发) → 用户刷到第 N 个时预刷新 N+4

### 菜单数据驱动
`config/menu.js` 定义所有菜单项，加新页面只需在此数组加一项。

### 收藏持久化
合集收藏时保存 title/description/tags/photo_paths/bgm_track 全套信息。
刷新/清空不丢收藏，`clearAllCollections` 仅删非收藏合集。

---

## 配置说明

### 数据库
- 地址：`DB_HOST` (见 .env)
- 用户：`root`
- 密码：`DB_PASSWORD` (见 .env)
- 数据库：`photo_manager_db`（自动创建）

### 图片目录
- 路径：`E:\图像`
- 格式：按日期文件夹整理 `YYYY-MM-DD`

### 本地 LLM
- 地址：`LOCAL_LLM_API` (见 .env)
- 模型：`google/gemma-4-26b-a4b`

### MiniMax API
- URL：`https://api.minimaxi.com/v1`
- 模型：`MiniMax-M2.7` (文本) / `MiniMax-VL-01` (视觉)

### BGM 曲库
- 位置：`frontend/public/bgm/`
- 来源：Mixkit 免费授权
- 数量：200 首 (5 种情绪 × 40 首)
- 格式：MP3

## 模型支持

| 模型 | 用途 | 来源 |
|------|------|------|
| 本地 Gemma 4-26b | 评分、描述、文案、合集文案 | 本地 (见 .env) |
| MiniMax M2.7 | 评分、描述、文案、合集文案 | API |
| MiniMax VL-01 | 图片视觉分析 | API |
| Edge-TTS (晓晓) | 系统语音播报 | 微软免费 |
